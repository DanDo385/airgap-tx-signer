/*
 * Placeholder main for Raspberry Pi Pico firmware.
 * Implemented after hardware arrival + Pico SDK install.
 */
#include "protocol.h"

/* Intentionally empty until Phase 2. */
int airgap_firmware_proto_version(void) { return AIRGAP_PROTO_VERSION; }
